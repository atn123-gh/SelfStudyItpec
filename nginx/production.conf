# # Django(Gunicorn)の8000番ポートとつなぐ
# upstream django {
#     # サーバにDjangoのコンテナ名を指定。今回はapp
#     # ポートはDjangoのコンテナの8000番ポート
#     server app:8000;
# }

# server {
#     listen 80;
#     server_name selfstudyitpec.com;
#     server_tokens off;

#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }

#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# server {
#     listen 443 ssl;
#     server_name selfstudyitpec.com;
#     server_tokens off;

#     ssl_certificate /etc/letsencrypt/live/selfstudyitpec.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/selfstudyitpec.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

#     location / {
#         proxy_pass  http://django;
#         proxy_set_header    Host                $http_host;
#         proxy_set_header    X-Real-IP           $remote_addr;
#         proxy_set_header    X-Forwarded-For     $proxy_add_x_forwarded_for;
#     }

#     # djangoの静的ファイル(HTML、CSS、Javascriptなど)を管理
#     location /static/ {
#         alias /staticfiles/;
#     }
# }
